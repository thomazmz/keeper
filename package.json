{
  "name": "@keeper/root",
  "version": "0.0.1",
  "scripts": {
    "test": "pnpm --recursive --if-present run test",
    "test:watch": "pnpm --recursive --if-present run test:watch",
    "build": "pnpm --recursive --if-present run build",
    "build:watch": "pnpm --parallel --if-present run build:watch",
    "start": "pnpm --recursive --if-present run start",
    "start:watch": "pnpm --parallel --if-present run start:watch"
  },
  "dependencies": {
    "@nodelith/config": "link:../nodelith/packages/config",
    "@nodelith/contract": "link:../nodelith/packages/contract",
    "@nodelith/controller": "link:../nodelith/packages/controller",
    "@nodelith/express": "link:../nodelith/packages/express",
    "@nodelith/http": "link:../nodelith/packages/http",
    "@nodelith/mongodb": "link:../nodelith/packages/mongodb"
  }
}